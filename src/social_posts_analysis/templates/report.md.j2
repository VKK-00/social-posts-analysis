# {{ title }}

Run ID: `{{ run_id }}`

Source snapshot:
- Platform: `{{ platform }}`
- Source: `{{ source_name }}`
- Source ID: `{{ source_id }}`
- Source type: `{{ source_type }}`
- Posts collected: `{{ post_count }}`
- Propagation instances: `{{ propagation_count }}`
- Comments collected: `{{ comment_count }}`
- Source runs merged: `{{ source_run_count }}`
- Embeddings provider: `{{ providers.get("embedding_provider", "n/a") }}`
- LLM provider: `{{ providers.get("llm_provider", "n/a") }}`

{% if telegram_summary %}
Telegram metrics:
- Linked discussion: `{{ telegram_summary.discussion_linked }}`
- Total views: `{{ telegram_summary.total_views }}`
- Total forwards: `{{ telegram_summary.total_forwards }}`
- Total replies: `{{ telegram_summary.total_reply_count }}`
- Filtered service messages: `{{ telegram_summary.filtered_service_message_count }}`
{% if telegram_summary.reaction_breakdown %}
- Reaction breakdown:
{% for row in telegram_summary.reaction_breakdown %}
  - `{{ row.reaction }}`: {{ row.count }}
{% endfor %}
{% endif %}
{% endif %}

{% if x_summary %}
X metrics:
- Total views: `{{ x_summary.total_views }}`
- Total likes: `{{ x_summary.total_likes }}`
- Total reposts: `{{ x_summary.total_reposts }}`
- Total quotes: `{{ x_summary.total_quotes }}`
- Total replies: `{{ x_summary.total_replies }}`
{% if x_summary.reaction_breakdown %}
- Metric breakdown:
{% for row in x_summary.reaction_breakdown %}
  - `{{ row.reaction }}`: {{ row.count }}
{% endfor %}
{% endif %}
{% endif %}

{% if threads_summary %}
Threads metrics:
- Total views: `{{ threads_summary.total_views }}`
- Total likes: `{{ threads_summary.total_likes }}`
- Total reposts: `{{ threads_summary.total_reposts }}`
- Total quotes: `{{ threads_summary.total_quotes }}`
- Total replies: `{{ threads_summary.total_replies }}`
{% if threads_summary.reaction_breakdown %}
- Reaction breakdown:
{% for row in threads_summary.reaction_breakdown %}
  - `{{ row.reaction }}`: {{ row.count }}
{% endfor %}
{% endif %}
{% endif %}

{% if instagram_summary %}
Instagram metrics:
- Total likes: `{{ instagram_summary.total_likes }}`
- Visible comments counter total: `{{ instagram_summary.total_comments_visible }}`
- Extracted comments: `{{ instagram_summary.total_comments_extracted }}`
- Reels detected: `{{ instagram_summary.reels_count }}`
{% if instagram_summary.reaction_breakdown %}
- Reaction breakdown:
{% for row in instagram_summary.reaction_breakdown %}
  - `{{ row.reaction }}`: {{ row.count }}
{% endfor %}
{% endif %}
{% endif %}

{% if propagation_summary %}
Propagation summary:
- Instances found: `{{ propagation_summary.total_instances }}`
- Instances with extracted comments: `{{ propagation_summary.with_comments }}`
- Extracted propagation comments: `{{ propagation_summary.extracted_comments }}`
- Visible propagation comment counter total: `{{ propagation_summary.visible_comment_total }}`
{% if propagation_summary.kinds %}
- By kind:
{% for row in propagation_summary.kinds %}
  - `{{ row.propagation_kind or "unknown" }}`: {{ row.count }}
{% endfor %}
{% endif %}
{% endif %}

{% if source_run_ids %}
Source runs:
{% for source_run_id in source_run_ids %}
- `{{ source_run_id }}`
{% endfor %}
{% endif %}

{% if warnings %}
## Data Quality Warnings
{% for warning in warnings %}
- {{ warning }}
{% endfor %}
{% endif %}

{% if source_warnings %}
## Source Warning Trace
| Source Run | Warning # | Warning |
| --- | ---: | --- |
{% for row in source_warnings %}
| `{{ row.source_run_id }}` | {{ row.warning_index }} | {{ row.warning }} |
{% endfor %}
{% endif %}

{% if source_run_trace %}
## Source Run Trace
| Source Run | Collector | Mode | Status | Fallback | Warnings |
| --- | --- | --- | --- | --- | ---: |
{% for row in source_run_trace %}
| `{{ row.source_run_id }}` | `{{ row.collector or "n/a" }}` | `{{ row.mode or "n/a" }}` | `{{ row.status or "n/a" }}` | `{{ row.fallback_used }}` | {{ row.warning_count }} |
{% endfor %}
{% endif %}

## Global Support By Side
| Side | Support | Oppose | Neutral | Unclear | Support Ratio | Net Support |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
{% for row in global_support %}
| `{{ row.side_id }}` | {{ row.support_count }} | {{ row.oppose_count }} | {{ row.neutral_count }} | {{ row.unclear_count }} | {{ "%.2f"|format(row.support_ratio) }} | {{ row.net_support }} |
{% endfor %}

## Origin Post Support
{% if origin_support %}
| Origin Post | Side | Support | Oppose | Neutral | Unclear | Support Ratio | Net Support |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
{% for row in origin_support %}
| `{{ row.scope_id }}` | `{{ row.side_id }}` | {{ row.support_count }} | {{ row.oppose_count }} | {{ row.neutral_count }} | {{ row.unclear_count }} | {{ "%.2f"|format(row.support_ratio) }} | {{ row.net_support }} |
{% endfor %}
{% else %}
- No direct origin-post support rows were computed.
{% endif %}

## Origin Plus Propagations Support
{% if origin_plus_support %}
| Origin Scope | Side | Support | Oppose | Neutral | Unclear | Support Ratio | Net Support |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
{% for row in origin_plus_support %}
| `{{ row.scope_id }}` | `{{ row.side_id }}` | {{ row.support_count }} | {{ row.oppose_count }} | {{ row.neutral_count }} | {{ row.unclear_count }} | {{ "%.2f"|format(row.support_ratio) }} | {{ row.net_support }} |
{% endfor %}
{% else %}
- No aggregated origin-plus-propagation support rows were computed.
{% endif %}

## Propagation Support
{% if propagation_support %}
| Propagation | Side | Support | Oppose | Neutral | Unclear | Support Ratio | Net Support |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
{% for row in propagation_support %}
| `{{ row.scope_id }}` | `{{ row.side_id }}` | {{ row.support_count }} | {{ row.oppose_count }} | {{ row.neutral_count }} | {{ row.unclear_count }} | {{ "%.2f"|format(row.support_ratio) }} | {{ row.net_support }} |
{% endfor %}
{% else %}
- No propagation support rows were computed.
{% endif %}

## Coverage Gaps
{% if coverage_gaps %}
{% for row in coverage_gaps %}
- Post `{{ row.post_id }}`: visible={{ row.comments_count }}, extracted={{ row.extracted_comment_count }}, gap={{ row.comment_gap }}
  {{ row.post_excerpt }}
{% endfor %}
{% else %}
- No major visibility gaps detected between visible comment counters and extracted comment texts.
{% endif %}

## Propagation Coverage Gaps
{% if propagation_coverage_gaps %}
{% for row in propagation_coverage_gaps %}
- Propagation `{{ row.propagation_id }}`: visible={{ row.comments_count }}, extracted={{ row.extracted_comment_count }}, gap={{ row.comment_gap }}
  {{ row.propagation_excerpt }}
{% endfor %}
{% else %}
- No major visibility gaps detected for propagated instances.
{% endif %}

## Reply Depth
| Depth | Comment Count |
| --- | ---: |
{% for row in reply_depth_summary %}
| `{{ row.depth }}` | {{ row.count }} |
{% endfor %}

## Top Post Narratives
{% for cluster in post_clusters %}
- `{{ cluster.cluster_id }}`: **{{ cluster.label }}** ({{ cluster.member_count }} items)
  {{ cluster.description }}
{% endfor %}

## Top Propagation Narratives
{% for cluster in propagation_clusters %}
- `{{ cluster.cluster_id }}`: **{{ cluster.label }}** ({{ cluster.member_count }} items)
  {{ cluster.description }}
{% endfor %}

## Top Comment Narratives
{% for cluster in comment_clusters %}
- `{{ cluster.cluster_id }}`: **{{ cluster.label }}** ({{ cluster.member_count }} items)
  {{ cluster.description }}
{% endfor %}

## Exemplar Quotes
{% for quote in exemplar_quotes %}
- `{{ quote.cluster_id }}` / **{{ quote.label }}**: {{ quote.text }}
{% endfor %}

## Top Supportive Comments
{% if top_supportive_comments %}
{% for row in top_supportive_comments %}
- `{{ row.comment_id }}` / side `{{ row.side_id }}` / confidence={{ "%.2f"|format(row.confidence) }} / depth={{ row.depth }}
  {{ row.text }}
{% endfor %}
{% else %}
- No high-confidence supportive comments were labeled for the configured sides.
{% endif %}

## Top Critical Comments
{% if top_critical_comments %}
{% for row in top_critical_comments %}
- `{{ row.comment_id }}` / side `{{ row.side_id }}` / confidence={{ "%.2f"|format(row.confidence) }} / depth={{ row.depth }}
  {{ row.text }}
{% endfor %}
{% else %}
- No high-confidence critical comments were labeled for the configured sides.
{% endif %}

## High Conflict Threads
{% for thread in high_conflict_threads %}
- Post `{{ thread.post_id }}` / side `{{ thread.side_id }}`: support={{ thread.support_count }}, oppose={{ thread.oppose_count }}, conflict={{ thread.conflict_score }}
  {{ thread.post_excerpt }}
{% endfor %}

## Top Propagated Items
{% if top_propagated_items %}
{% for row in top_propagated_items %}
- Origin `{{ row.origin_post_id }}`: instances={{ row.propagation_count }}, propagation_comments={{ row.propagation_comment_count }}
  {{ row.origin_excerpt }}
{% endfor %}
{% else %}
- No propagated instances were linked back to origin posts in this run.
{% endif %}

## Comments Under Propagations
{% if propagation_comments %}
{% for row in propagation_comments %}
- Propagation `{{ row.parent_entity_id }}` / origin `{{ row.origin_post_id }}` / kind `{{ row.propagation_kind or "unknown" }}` / depth={{ row.depth }}
  {{ row.comment_excerpt }}
{% endfor %}
{% else %}
- No extracted comments were attached to propagated instances in this run.
{% endif %}

## Language Mix
| Item Type | Language | Count |
| --- | --- | ---: |
{% for row in language_mix %}
| `{{ row.item_type }}` | `{{ row.language }}` | {{ row.count }} |
{% endfor %}
